import { InputLettersDirective } from './input-letters.directive';

describe('InputLettersDirective', () => {
  it('should create an instance', () => {
    const directive = new InputLettersDirective();
    expect(directive).toBeTruthy();
  });
});
